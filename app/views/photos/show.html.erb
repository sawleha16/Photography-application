<% @album.photos.each do |photo|%>
  <%= image_tag(photo.image, size: "400x400") %></a>
    <li><%= link_to "update", "/albums/#{@album.id}/photos/#{photo.id}/edit"%></li>
<p>
  <strong>Caption:</strong>
  <%= photo.caption%>
</p>
<% end %>
